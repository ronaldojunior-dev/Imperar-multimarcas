Coloque o arquivo do logo nesta pasta com o nome `logo.png` ou `logo.svg`.

O `index.html` referencia `assets/logo.png` como logotipo.

Se preferir outro nome, atualize o atributo `src` da tag `<img>` no arquivo `index.html`.
